Engine

  BUG: There are issues with multiple embeds on a page
  Enable fastclick for mobile users to tap straight away
  Add line width as a configurable item
  Add "appear at top" and "appear at bottom" support, so not all annotations have to appear below
  Vertical line is 1px too short
  Make desktop font larger, and possibly courier

Builder

  Remove colour picker, offer brand colours on a swatch with opacity configuration only
  Ability to host images on BuzzFeed's servers
  Ability to edit or delete annotations which have been entered
  Ability to edit an entire existing embed
  Ability to configure an annotation to "appear at top" or "appear at bottom"
  Being able to easily bold, ital, or hyperlink within the annotations w/o using HTML.
  Ability to move the annotation point w/mouse after it's created vs using X and Y coordinates
  A Highlighter style for annotating a phrase or line.